apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "chatbot-platform.fullname" . }}
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "chatbot-platform.fullname" . }}
                port:
                  number: {{ .Values.service.port }}
